{
  "name": "react-jsonserver-vercel",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "build-frontend": "cd frontend && npm install && npm run build && cp -r dist ../static"
  },
  "dependencies": {
    "@upstash/redis": "^1.35.4",
    "express": "^4.18.2",
    "json-server": "^0.17.3",
    "multer": "^2.0.2"
  }
}
